spring:
  # -------------------------
  # Redis 配置
  # -------------------------
  redis:
    database: 3         # 使用不同的数据库

  config:
    import: classpath:application-dev-common.yml

# -------------------------
# JWT 配置（商户端）
# -------------------------
foodie:
  jwt:
    merchant-secret-key: foodie-merchant-20260109-secret-key
    merchant-ttl: 7200000
    merchant-token-name: merchant-token
  upload:
    service-path: merchant
