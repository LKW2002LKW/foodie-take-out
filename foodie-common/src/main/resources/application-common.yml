foodie:
  minio:
    endpoint: ${MINIO_ENDPOINT:http://192.168.1.11:9000}
    accessKey: ${MINIO_ACCESS_KEY:admin}
    secretKey: ${MINIO_SECRET_KEY:lkw2468LKW@}
    bucket: ${MINIO_BUCKET:foodie-images}
    publicUrl: ${MINIO_PUBLIC_URL:http://192.168.1.11:9000/foodie-images}
    basePath: ${MINIO_BASE_PATH:upload}
    maxSize: ${MINIO_MAX_SIZE:5}
    allowedTypes:
      - jpg
      - jpeg
      - png
      - gif
      - webp
