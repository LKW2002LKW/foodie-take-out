spring:
  # -------------------------
  # 应用名称
  # -------------------------
  application:
    name: foodie-user-server

  config:
    import: classpath:application-common-base.yml

  # -------------------------
  # 服务器端口
  # -------------------------
server:
  port: 8083

foodie:
  cache:
    merchant-status-ttl-hours: 24

