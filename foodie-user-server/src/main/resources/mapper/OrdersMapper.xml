<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodie.user.mapper.OrdersMapper">

    <!-- 用户订单分页查询-我的评价-待评价 -->
    <select id="pageQuery" resultType="com.foodie.vo.user.OrderVO">
        SELECT
            o.id,
            o.order_number,
            o.user_id,
            o.merchant_id,
            o.status,
            o.order_time,
            o.total_amount
        FROM orders o
        <where>
            o.user_id = #{userId}
            <if test="query.pendingReviewOnly != null and query.pendingReviewOnly == true">
                AND o.status = 5
                AND NOT EXISTS (
                    SELECT 1
                    FROM order_review r
                    WHERE r.order_id = o.id
                )
            </if>
            <if test="(query.pendingReviewOnly == null or query.pendingReviewOnly == false) and query.status != null and query.status != 0">
                AND o.status = #{query.status}
            </if>
        </where>
        ORDER BY o.order_time DESC
    </select>

    <!-- 查询订单详情 -->
    <select id="getOrderDetail" resultType="com.foodie.vo.user.OrderVO">
        SELECT
            o.id,
            o.order_number,
            o.user_id,
            o.merchant_id,
            m.merchant_name,
            o.status,
            o.order_time,
            o.checkout_time,
            o.pay_method,
            o.pay_status,
            o.amount,
            o.delivery_fee,
            o.pack_amount,
            o.total_amount,
            o.remark,
            o.phone,
            o.address,
            o.consignee,
            o.estimated_delivery_time,
            o.delivery_time,
            o.tableware_number
        FROM orders o
                 LEFT JOIN merchant m ON o.merchant_id = m.id
        WHERE o.order_number = #{orderNumber}
          AND o.user_id = #{userId}
    </select>

</mapper>
