<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodie.user.mapper.SetmealMapper">

    <!-- 根据商户和分类查询套餐 -->
    <select id="listByMerchantAndCategory" resultType="com.foodie.vo.user.SetmealVO">
        SELECT
        id,
        merchant_id,
        category_id,
        name,
        price,
        image,
        description
        FROM setmeal
        WHERE merchant_id = #{merchantId}
        AND status = 1
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 查询套餐详情 -->
    <select id="getSetmealDetail" resultType="com.foodie.vo.user.SetmealVO">
        SELECT
            id,
            merchant_id,
            category_id,
            name,
            price,
            image,
            description
        FROM setmeal
        WHERE id = #{setmealId}
          AND status = 1
    </select>

</mapper>
