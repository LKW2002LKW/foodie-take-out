<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodie.user.mapper.DishMapper">

    <!-- 根据商户和分类查询菜品 -->
    <select id="listByMerchantAndCategory" resultType="com.foodie.vo.user.DishVO">
        SELECT
        id,
        merchant_id,
        category_id,
        name,
        price,
        image,
        description
        FROM dish
        WHERE merchant_id = #{merchantId}
        AND status = 1
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 查询菜品详情 -->
    <select id="getDishDetail" resultType="com.foodie.vo.user.DishVO">
        SELECT
            id,
            merchant_id,
            category_id,
            name,
            price,
            image,
            description
        FROM dish
        WHERE id = #{dishId}
          AND status = 1
    </select>

</mapper>